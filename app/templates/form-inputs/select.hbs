{{view 'select'
  id=inputId
  invalid=isInvalid

  content=content
  name=name
  value=value
  selection=selection
  prompt=prompt
  disabled=disabled
  required=required
  optionValuePath=optionValuePath
  optionLabelPath=optionLabelPath
}}
